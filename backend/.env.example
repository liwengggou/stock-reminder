# Server
PORT=3001
NODE_ENV=development  # Change to 'production' for deployment

# Database (PostgreSQL - Neon)
# Get your connection string from: https://neon.tech
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# JWT
# IMPORTANT: Generate a secure secret for production:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS in production)
# FRONTEND_URL=https://YOUR-IP.nip.io

# Email - Choose ONE option below:

# Option 1: QQ Mail (Recommended for China)
# 1. Login to mail.qq.com
# 2. Settings -> Account -> POP3/SMTP service -> Enable
# 3. Get authorization code (授权码) - this is your SMTP_PASS
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USER=your-qq-number@qq.com
SMTP_PASS=your-qq-authorization-code
EMAIL_FROM=your-qq-number@qq.com

# Option 2: 163 Mail (NetEase)
# 1. Login to mail.163.com
# 2. Settings -> POP3/SMTP/IMAP -> Enable
# 3. Get authorization code
# SMTP_HOST=smtp.163.com
# SMTP_PORT=465
# SMTP_USER=your-email@163.com
# SMTP_PASS=your-163-authorization-code
# EMAIL_FROM=your-email@163.com

# Stock Price API (Finnhub - fallback when Yahoo Finance fails)
# 1. Sign up at https://finnhub.io/
# 2. Get your free API key from the dashboard
FINNHUB_API_KEY=your-finnhub-api-key
